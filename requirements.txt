# agent_engine – Phase 1 & 2 dependencies
# Core Win32 interaction for Phase 1 uses ctypes (stdlib).
# Phase 2 UI element detection requires uiautomation:

uiautomation>=2.0.29

# Type checking (dev only)
mypy>=1.9
